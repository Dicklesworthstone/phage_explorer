// Thin alias to the shared ScatterCanvas implementation so overlays reuse the
// same primitive as the rest of the web app.
export {
  ScatterCanvas as default,
  ScatterCanvas,
  type ScatterCanvasProps,
} from '../../primitives/ScatterCanvas';
